{
  "name": "caelon-desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Caelon Portal desktop shell (Tauri v2)",
  "packageManager": "pnpm@10.11.1",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:mac-arm": "tauri build --target aarch64-apple-darwin",
    "icon": "node ./scripts/make-icon.mjs && tauri icon ./scripts/icon-source.png",
    "tauri": "tauri",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
